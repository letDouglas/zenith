apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: zenith-postgres
  namespace: database
spec:
  instances: 3

  storage:
    size: 2Gi
    storageClass: longhorn

  bootstrap:
    initdb:
      database: app_db
      owner: app_user
